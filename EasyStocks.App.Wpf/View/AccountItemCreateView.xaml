<UserControl x:Class="EasyStocks.View.AccountItemCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:easyStocks="clr-namespace:EasyStocks;assembly=EasyStocks"
             xmlns:platform="clr-namespace:EasyStocks.Platform"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <ContentControl x:Name="AccountData"/>
    <Button
    Grid.Row="1"
    VerticalAlignment="Bottom"
    Content="{x:Static easyStocks:EasyStocksStrings.AddToPortfolio}"
    x:Name="CreateAccountItem"
    Margin="20"
    Foreground="{x:Static platform:CustomColors.OkBrush}"/>
    <Button
    Grid.Row="2"
    VerticalAlignment="Bottom"
    Content="{x:Static easyStocks:EasyStocksStrings.Cancel}"
    x:Name="Cancel"
    Margin="20"/>
  </Grid>
</UserControl>
