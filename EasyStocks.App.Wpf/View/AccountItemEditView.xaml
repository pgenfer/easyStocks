<UserControl x:Class="EasyStocks.View.AccountItemEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:easyStocks="clr-namespace:EasyStocks;assembly=EasyStocks"
             xmlns:platform="clr-namespace:EasyStocks.Platform"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="45"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <ContentControl x:Name="AccountData"/>
    <Button
       Grid.Row="1"
       Content="{x:Static easyStocks:EasyStocksStrings.Remove}"
       x:Name="RemoveAccountItem"
       Margin="10"
       HorizontalAlignment="Right"
       Width="100"
       Foreground="{x:Static platform:CustomColors.FailureBrush}"/>
    <Button
       Grid.Row="2"
       VerticalAlignment="Bottom"
       Content="{x:Static easyStocks:EasyStocksStrings.Ok}"
       x:Name="ConfirmChanges"
       Margin="20"/>
  </Grid>
</UserControl>
